/*
 * UsersTable Messages
 *
 * This contains all the text for the UsersTable container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UsersTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UsersTable container!',
  },
});
